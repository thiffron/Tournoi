name = "tournoi"
compatibility_date = "2026-05-27"

[assets]
directory = "./"
not_found_handling = "single-page-application"

# Connect the Worker to the production custom domain. On deploy, Cloudflare
# provisions the DNS record + certificate automatically (no manual DNS needed).
[[routes]]
pattern = "tournoi-carte.com"
custom_domain = true
